AI-PROXY-GATEWAY (PYTHON)

1. REGISTER the model from diff api provider
2. AFTER REGISTER the prxoy should be accesible to the user for the user (exposing api endpoint , get a response from this model)
3. Take request and route it to the correct model


--> Authenticate 
--> validate token (JWT token)
--> Authorisation
openAI/Claude provider -> api call to the provider []  


--> taking the req -> return respon -> capture metric -> (input token , output token , latency)

--[]
streaming response 


-> can use in memory for support 
